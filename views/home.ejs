<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <% if(!locals.posts) { %>
    <link rel="stylesheet" href="/styles/homeStyle.css">
    <% } %>
  </head>
  <%- include("./partials/header.ejs") %>
  <% if(locals.posts) { %>          <%# checks if there is post's. if there is display them%>
  <body class="<%= !posts || posts.length === 0 ? 'no-posts-body' : '' %>"> 
    <div class="container">
    <div class="row">

    <% posts.forEach(post => { %>
    <div class="col-md-6 mb-4">
      <div class="row g-0 border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
        <div class="col p-4 d-flex flex-column position-static">
          <strong class="d-inline-block mb-2 text-primary-emphasis"><%= post.topic %></strong>
          <h3 class="mb-0"> <%= post.title %> </h3>
          <div class="mb-1 text-body-secondary"> <%= post.publishDate %> </div>
          <p class="card-text mb-auto"><%= post.teaserText %></p>
          <a href="/post/<%= post.id %>" class="icon-link gap-1 icon-link-hover stretched-link">
            Continue reading
            <svg class="bi"><use xlink:href="#chevron-right"></use></svg>
          </a>
        </div>
        <% if (post.pic) { %>
            <div class="col-auto d-none d-lg-block">
                <img
                  src="./uploads/<%= post.pic%>"
                  alt="Thumbnail"
                  width="200"
                  height="250"
                  style="object-fit: cover;"
                >
            </div>
        <% } %>
      </div>
    </div>
    <% }); %>
    <a href="/makepost" class="btn btn-primary">Create New Post</a>
    </div>
    </div>
    <% } else {%> <%# display if there's no posts%>
        <div class="no-posts-container">
            <h1>No Posts Available</h1>
            <p>There are currently no posts to display. Please check back later or .</p>
            <a href="/makepost" class="btn btn-primary">Create New Post</a>
        </div>
    <% } %>
    <%- include("./partials/footer.ejs") %>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
